#include <stdio.h>
int main() {
    double A,B,C,mp;
    double PA=2,PB=3,PC=5;
    scanf("%lf %lf %lf",&A,&B,&C);
    mp=((A*PA)+(B*PB)+(C*PC))/(PA+PB+PC);
    printf("MEDIA = %.1lf\n",mp);
    return 0;
}
