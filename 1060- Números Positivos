#include <stdio.h>
int main() {
    double n;
    int i=0;
    int soma=0;
    for(i=0;i<6;i++)
    {
        scanf("%lf\n",&n);
        if(n>0)
         {
             soma=(soma+1);
         }
    }
    printf("%d valores positivos\n",soma);
    return 0;
}
    
