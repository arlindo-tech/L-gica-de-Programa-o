//numeros impares
#include <stdio.h>
int main(void)
{
    int X,i;//valor inicial e valores impares no intervalo de 1 a i
    scanf("%d",&X);
    for(i=1;i<=X;i++)
    {
        if((i%2)!=0)
        {
            printf("%d\n",i);
        }
    }
    return 0;
}
