//resto da divisao
#include <stdio.h>
int main() {
    int x,y;//intervalo dos valores a serem avaliados
    int i;//valores que o resto e 2 ou 3
    scanf("%d %d",&x,&y);
    if(x>0 && y>0)
    {
        if(x<y)
        {
          for(i=x+1;i<y;i++)
          {
            if((i%5)==2 || (i%5)==3)
            {
              printf("%d\n",i);
            }
          } 
        }
        else
        {
          for(i=y+1;i<x;i++)
          {
            if((i%5)==2 || (i%5)==3)
            {
              printf("%d\n",i);
            }
          }
        }
    }
    return 0;
}
