#include <stdio.h>
 
int main() {
 
    int tempo,vm,distancia;
    double litros;
    scanf("%d %d",&tempo,&vm);
    distancia = tempo*vm;
    litros = distancia/12.0;
    printf("%.3lf\n",litros);
    return 0;
}
